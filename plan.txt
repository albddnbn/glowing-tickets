gui program which will hold custom ticket replies as text files

program will start up and present screen to choose - replies or add reply

    - replies - will open a nwe window or change current window to onethat has buttons for all the replies the user has created, program will use the text files to create the interface, so your replies will get saved across sessions

        - create reply - will allow user to add new reply, which will create new text file which will generate a new buttton on the 'replies' page


        above the buttons there could be a textbox where you could type or paste the users name to insert into the reply